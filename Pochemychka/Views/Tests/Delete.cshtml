@model Pochemychka.Models.Test

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Test</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NameTest)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NameTest)
        </dd>
    </dl>
    
    <form asp-action="Delete" asp-route-name="@ViewBag.UserName" asp-route-email="@ViewBag.UserEmail" asp-route-isAdmin="@ViewBag.isAdmin">
        <input type="hidden" asp-for="IdTest" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
